import { message } from 'antd';

/**
 * SxwlMessage — 基于 antd message 的二次封装
 *
 * 用法：
 * ```tsx
 * SxwlMessage.success('操作成功');
 * SxwlMessage.error('操作失败');
 * ```
 */
const SxwlMessage = message;

export default SxwlMessage;
