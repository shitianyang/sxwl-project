<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sxwl.system.mapper.SysMonitorDbLogMapper">

    <insert id="insert" parameterType="com.sxwl.system.model.entity.SysMonitorDbLog">
        INSERT INTO sys_monitor_db_log (id, active_connections, create_time)
        VALUES (#{id}, #{activeConnections}, #{createTime})
    </insert>

    <select id="selectRecent" resultType="com.sxwl.system.model.entity.SysMonitorDbLog">
        SELECT id, active_connections AS activeConnections, create_time AS createTime
        FROM sys_monitor_db_log
        ORDER BY create_time DESC
        LIMIT #{limit}
    </select>

</mapper>
